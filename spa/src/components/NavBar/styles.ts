import { makeStyles } from '@mui/styles';

export const useStyle = makeStyles(() => ({
    nav: {
        height: '100vh',
        width: '200px',
        background: '#262626',
        color: '#FFFFFF',
    },
}));
